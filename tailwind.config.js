/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // class를 활용하여 darkmode 적용을 위함
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}